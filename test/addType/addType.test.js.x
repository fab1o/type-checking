// ***
// import { addType } from '../../src';

// describe.skip('addType', () => {
//     it('validator is expected', () => {
//         expect(() => {
//             addType('blueberry');
//         }).toThrow(
//             'addType(name, validator, options) validator expected a Function that returns boolean.'
//         );
//     });

//     it('options is expected an object', () => {
//         expect(() => {
//             addType('blueberry', () => false, null);
//         }).toThrow(
//             'addType(name, validator, options) options expected an Object or undefined.'
//         );
//     });

//     it('options is expected a valid string', () => {
//         expect(() => {
//             addType('', () => false, null);
//         }).toThrow('addType(name, validator, options) name expected a non-empty String.');
//     });
// });
